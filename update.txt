著作権の年号を2021年に修正する。
Change copyright to 2021.

テーブルセル内のフォントサイズを変更する。
Change font size within table cells.

プライベートのメンバ変数を、get-onlyプロパティに変更する。
Change private member variables to get-only property.

ページのタイトルを変更する。
Change the title of the page.

ドキュメント内の-sオプションの使用法を変更する。
Change -s option usage in docs.

ヘッダーの色を暗くする。
Darken the header colours.

パフォーマンス向上のため、Tomcatでコネクションプールを有効化する。
Enable connection pooling on Tomcat for performance improvement.

ナビゲーションのバグを修正する。（※Fixはバグ修正とtypoに使う）
Fix a bug in the navigation.

テストを修正する。
Fix a test.

H1タグ内のリンクタグを修正する。
Fix anchor tag inside h1 tag.

グローバルナビゲーションのリンクタグを修正する。
Fix anchor tag of global navigation.

HTMLファイルのパスを修正する。
Fix html file paths.

誤字を修正する。
Fix typos.

格子の描画動作を改善する。
Improve grid drawing behavior.

ブラウザのサポートを改善する。
Improve browser support.

テキストボックスとレイアウトフレームの間の左パディングを増やす。
Increase left padding between textbox and layout frame.

ヘッダーの色を明るくする。
Lighten the header colours.

トランザクション処理をより高速化する。
Make it faster to process transactions.

GrayConverterクラスのToGrayScaleメソッドをパブリックにする。
Make GrayConverter.ToGrayScale() public.

メンバ変数をプライベートにする。
Make member variables private.

画像ファイルをimgフォルダに移動する。
Move image files to img folder.

MainLogic.SortメソッドをDepatureTimeLogicへ移動する。
Move MainLogic.Sort() to DepatureTimeLogic.

src/DepatureTime.csをsrc/model以下に移動する。
Move src/DepatureTime.cs to src/model/DepatureTime.cs.

（テーブル中の）列ヘッダーの名称を変更する。
Rename column header.

コンストラクタの引数名を変更する。
Rename constructor parameter.

ファイル名をping.shからping_checker.shに変更する。
Rename ping.sh -> ping_checker.sh.
Rename ping.sh to ping_checker.sh.
ping.sh -> ping_checker.sh.

テーブルタグのパディングを設定する。
Set the padding of table tag.

テーブルのセルの境界線（枠線）の幅を設定する。
Set the width of a table cell border.

DepatureTimeLogic.Sortメソッドを簡素化（スリム化）する。
Simplify DepatureTimeLogic.Sort().

HTMLの読み込みを簡素化する。
Simplify html loading.

itemsをitem IDでソートする。
Sort items by the item ID.

itemsを価格でソートする。
Sort items by the price.

アルファベット順にソートする。
Sorty by alphabetical order.

Java-VMをJava-12に更新する。（※Updateはバグ以外の修正）
Update JVM to Java-12.

readme.txtを更新する。
Update readme.txt.

コメントを修正する。
Update comment.

Commons DBCPの代わりにTomcat DBCPを用いる。
Use Tomcat DBCP instead of Commons DBCP.